from django.apps import AppConfig

class TrafficConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.traffic'
    verbose_name = 'CampusPulse Traffic & Computer Vision Vehicle Detection'
